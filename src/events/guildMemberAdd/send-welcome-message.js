
/**
 * 
 * @param {import('discord.js').GuildMember} guildMember 
 */

module.exports = async (guildMember) => {
    try {
        
    } catch (error) {
        console.log(`⚠️  ${__filename}:\n${error}`);
    }
}